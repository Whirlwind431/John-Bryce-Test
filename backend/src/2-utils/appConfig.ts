class AppConfig {

    //Database
    public host = "localhost"
    public user = "root"
    public password = ""
    public database = "meetings_jb_test"

    // server port
    public port = 3001

}

const appConfig = new AppConfig()

export default appConfig
